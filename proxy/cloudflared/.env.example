# Cloudflare Tunnel Configuration
#
# SETUP INSTRUCTIONS:
# 1. Go to Cloudflare Zero Trust(https://one.dash.cloudflare.com/) Dashboard -> Networks -> Tunnels
# 2. Create a tunnel and navigate to the "Install and run connectors" section
# 3. Choose your environment (e.g., Docker) and copy the token from the command (the value after --token)
# 4. Set the copied value in the CLOUDFLARE_TUNNEL_TOKEN variable below
# TUNNEL_ID from step 3 (for reference):

#TUNNEL_ID=your-tunnel-id-here
CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here  